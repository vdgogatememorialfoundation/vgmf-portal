version: "3.8"

services:
  vgmf-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vgmf-portal
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://neondb_owner:npg_9hxQJKw8GNsg@ep-bold-silence-adtcvru2-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
      - AUTH_SECRET=vgmf-secure-key-2026-change-me
      - NEXTAUTH_URL=https://vaidyagogate.org
      - NEXTAUTH_SECRET=vgmf-secure-key-2026-change-me
      - RAZORPAY_KEY_ID=
      - RAZORPAY_KEY_SECRET=
      - SHIPROCKET_EMAIL=
      - SHIPROCKET_PASSWORD=
      - OPENAI_API_KEY=
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
